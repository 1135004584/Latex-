{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile&&open Latex",
            "type": "shell",
            "command": "taskkill -f -im AcroRd32.* & xelatex test.tex && explorer test.pdf",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}